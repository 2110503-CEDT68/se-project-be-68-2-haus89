@startuml Get My Booking (GET)

header Dentist Booking System - Bookings
footer Page %page% of %lastpage%
title "Get My Booking (GET)"

participant "Client" as client
participant "<<javaScript>>\n:server" as server
participant "<<router>>\n:bookings" as routerBookings
participant "<<controllers>>\n:bookings" as controllersBookings
participant "<<model>>\n:Booking" as modelBooking
database "<<MongoDB>>\n:bookings" as BookingsDatabase

client->server ++:req.get('/api/v1/bookings/me')
server->routerBookings ++:app.use('/api/v1/bookings',bookings)
routerBookings -> controllersBookings ++:getMyBooking()
controllersBookings->modelBooking ++:findOne({user: req.user.id}).populate('dentist')
modelBooking ->BookingsDatabase ++: find booking with dentist info
BookingsDatabase --> modelBooking --: booking
controllersBookings <-- modelBooking --:booking

alt booking not found
    controllersBookings->client --:error {success: false, message: 'No booking found'}
else booking found
    controllersBookings->client --:response {success: true, data: booking}
end

@enduml
