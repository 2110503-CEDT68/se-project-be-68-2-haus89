@startuml Get All Users (GET)

header Dentist Booking System - Users
footer Page %page% of %lastpage%
title "Get All Users (GET) - Admin Only"

participant "Client" as client
participant "<<javaScript>>\n:server" as server
participant "<<router>>\n:users" as routerUsers
participant "<<controllers>>\n:users" as controllersUsers
participant "<<model>>\n:User" as modelUser
database "<<MongoDB>>\n:users" as UsersDatabase

client->server ++:req.get('/api/v1/users')
server->routerUsers ++:app.use('/api/v1/users',users)
routerUsers -> controllersUsers ++:getUsers()
controllersUsers->modelUser ++:find()
modelUser ->UsersDatabase ++: find all users
UsersDatabase --> modelUser --: users
controllersUsers <-- modelUser --:users
controllersUsers->client --:response {success: true, count, data: users}

@enduml
